version: '3.5'

services:
  sistema-tjmt-jus-br:
    image: ${DOCKER_REGISTRY}sistema.tjmt.jus.br:${BRANCH:-develop}.${VERSION:-latest}
    labels:
      kompose.service.expose: sistema-stage.tjmt.jus.br
    container_name: sistema-stage.tjmt.jus.br

networks:
  default:
    name: ns-sistema-stage
